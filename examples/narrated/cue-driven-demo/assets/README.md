# Demo assets

No binary media is committed.

The fixture assembly commands generate a 24-second 220 Hz synchronization tone
at `remotion/public/assets/demo-tone.wav`. It exists only to prove that the
Remotion composition carries embedded local audio through render and final mix.
It is not speech or narration and must never receive a pronunciation pass.
