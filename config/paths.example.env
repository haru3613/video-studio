# Optional operator-owned paths; never put credentials in this file.
# Keep the active workspace outside this source checkout.
VIDEO_STUDIO_WORKSPACE="$HOME/VideoStudio"
VIDEO_STUDIO_RUNTIME_STATE="$HOME/.local/state/video-studio/runtime"
# The bundled media-tools directory is the default tools root.
# Provider keys are supplied separately through environment or a protected file.
